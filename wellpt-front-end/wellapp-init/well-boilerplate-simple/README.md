# wellapp-boilerplate-simple

wellsoft的web应用范例

## 安装说明

**不用直接使用 `npm i` 安装**

```bash
$ npm i -g well-init
$ well-init --type simple wellapp-sample
$ cd wellapp-sample
$ npm i
$ npm run dev
$ open http://localhost:7001/ping
```

