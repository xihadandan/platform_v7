表单从表隐藏列时，控件不会创建控件bug
http://zen.well-soft.com:81/zentao/bug-view-29570.html

Index: WebContent/resources/bootstrap-table/1.11.0/js/bootstrap-table.js
===================================================================
--- WebContent/resources/bootstrap-table/1.11.0/js/bootstrap-table.js	(revision 194171)
+++ WebContent/resources/bootstrap-table/1.11.0/js/bootstrap-table.js	(working copy)
@@ -56,7 +56,7 @@
     };
 
     // http://jsfiddle.net/wenyi/47nz7ez9/3/
-    var setFieldIndex = function (columns) {
+    var setFieldIndex = function (columns, hideInvisible) {
         var i, j, k,
             totalCol = 0,
             flag = [];
@@ -64,6 +64,15 @@
         for (i = 0; i < columns[0].length; i++) {
             totalCol += columns[0][i].colspan || 1;
         }
+        if(hideInvisible && columns.length > 1) {
+        	var lastColumns = columns[columns.length-1];
+        	for (i = 0; i < lastColumns.length; i++) {
+        		if(lastColumns[i].visible){
+        			continue;
+        		}
+        		totalCol += lastColumns[i].colspan || 1;
+        	}
+        }
 
         for (i = 0; i < columns.length; i++) {
             flag[i] = [];
@@ -673,7 +682,7 @@
         }
         this.columns = [];
 
-        setFieldIndex(this.options.columns);
+        setFieldIndex(that.options.columns, that.options.hideInvisible);
         $.each(this.options.columns, function (i, columns) {
             $.each(columns, function (j, column) {
                 column = $.extend({}, BootstrapTable.COLUMN_DEFAULTS, column);
@@ -797,7 +806,9 @@
                     that.header.cellStyles[column.fieldIndex] = column.cellStyle;
                     that.header.searchables[column.fieldIndex] = column.searchable;
 
-                    if (!column.visible) {
+                    if(!column.visible && that.options.hideInvisible){
+                    	style = "display:none;";
+                    }else if (!column.visible) {
                         return;
                     }
 
@@ -1706,7 +1717,10 @@
                     return;
                 }
 
-                if (!column.visible) {
+                if(!column.visible && that.options.hideInvisible){
+                	// 隐藏不可见,用于创建表单控件
+                	that.header.styles[j] = "display:none;";
+                } else if (!column.visible) {
                     return;
                 }
 
