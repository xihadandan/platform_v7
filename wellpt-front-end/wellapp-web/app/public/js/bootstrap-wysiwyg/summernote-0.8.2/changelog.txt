老版本

1、第3969行Editor的currentStyle方法增加记录lastRange
this.lastRange = rng;

2、第1643行summernote增加insertText、insertHtml方法
this.insertText = function(text) {
	...
};

this.insertHtml = function(html) {
	...
};

升级新版本到0.8.10
4129行 Editor.prototype.currentStyle 方法增加记录lastRange
this.lastRange = rng;

6922行 添加insertText， insertHtml方法
Context.prototype.insertText = function( text ){

}
Context.prototype.insertHtml = function( text ){
}

