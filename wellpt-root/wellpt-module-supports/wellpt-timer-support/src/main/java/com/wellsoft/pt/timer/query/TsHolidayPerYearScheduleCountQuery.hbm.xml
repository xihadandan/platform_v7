<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<hibernate-mapping>
    <sql-query name="holidayPerYearScheduleCountQuery">
        <![CDATA[
		select t.year as year, count(*) as count
		from ts_holiday_schedule t
		where t.system_unit_id = :systemUnitId
		<if isNotBlank(systemId)>
		    and t.system = :systemId
		</if>
		group by t.year
		order by t.year
	]]>
    </sql-query>
</hibernate-mapping>