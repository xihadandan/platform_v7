<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<hibernate-mapping>

    <!-- 获取新建组织时对应的归属单wselect2使用 -->
    <sql-query name="queryUnitListForSelect2">
        <![CDATA[
		select e.id, e.name
		from multi_org_system_unit e
		where 1 = 1
		<if isNotBlank( name )>
			and e.name like '%${name}%'
		</if>
		<if isNotBlank( excludeIds )>
			and e.id not in ( :excludeIds )
		</if>
	]]>
    </sql-query>


</hibernate-mapping>