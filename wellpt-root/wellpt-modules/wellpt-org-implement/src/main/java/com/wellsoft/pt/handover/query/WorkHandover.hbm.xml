<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<hibernate-mapping>

    <!-- 组织管理_工作交接数据仓库 -->
    <sql-query name="queryWorkHandoverDatas">
        <![CDATA[
		select *
		from WH_WORK_HANDOVER wh
		where  1 = 1
		AND wh.system_unit_id = :systemUnitId 
		<if isNotBlank( whereSql )>
		AND ${whereSql}
		</if>
		
		<if isNotBlank( orderStr )>
		    ${orderStr}
		 <else>
		 order by wh.CREATE_TIME desc
		</if>
		
	]]>
    </sql-query>
    <sql-query name="getTodoWorkHandoverList">
        <![CDATA[
		select *
		from WH_WORK_HANDOVER wh
		where  1 = 1
		and WORK_HANDOVER_STATUS = 1
		 order by wh.CREATE_TIME asc
		</if>
		
	]]>
    </sql-query>


</hibernate-mapping>