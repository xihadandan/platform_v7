#模块说明
这是一个POM工程，没有实际承载业务，只是一个分类作用。

该目录结果主要用于存放平台各模块

这下面的模块主要有两类
1. 非implement结尾的工程,完整的模块代码,可作为其他工程依赖的jar工程，
2. implement结尾的工程,不完成的模块代码，还有部分在声明工程support工程中， 不可作为其他工程依赖的jar工程，只能用其support工程作为其他工程的依赖jar工程
