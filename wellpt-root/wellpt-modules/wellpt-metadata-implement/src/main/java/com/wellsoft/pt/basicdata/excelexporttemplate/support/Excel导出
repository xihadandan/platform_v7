表单导出EXCEL规范：(其他不导出Excel的表单不受约束)
功能：
1、支持按模版表空间下标和表空间名称导出,默认导出第一个表空间，导出的文件名称为表空间名称
2、支持导出的显示值的处理，包括name处理和class处理，详情见ClazzProcessor和NameProcessor
3、

约束：
1、整个表单（包括从表）,不能有重复的英文字段,比如：主表有字段性别(xb),从表有字段xb则不行。这是因为Excel中，单元格名称必须唯一的约束
解决办法：定义字段时注意
2、表单字段名与Excel单元格名称必须一致，从表设置表头即可，序号不需要管理
3、从表结束但紧接在后面的行又还有不是从表的内容时要标识用“uf_end”标识结束，否则有可能覆盖主表内容

问题：
1、子表自动添加行是，单元格合并可能有问题，建议做模版时留够足够过的子表行